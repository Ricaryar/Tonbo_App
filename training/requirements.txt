# Tonbo 离线 LLM 微调依赖
# 安装: pip install -r requirements.txt

torch>=2.1.0
transformers>=4.40.0
peft>=0.10.0
datasets>=2.18.0
accelerate>=0.28.0
bitsandbytes>=0.43.0; platform_system != "Windows" or platform_machine != "x86_64"
safetensors>=0.4.0
huggingface_hub>=0.22.0
sentencepiece>=0.2.0
trl>=0.8.0
gguf>=0.10.0
